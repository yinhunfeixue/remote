package remote2.components
{
	/**
	 * 模式窗口阻断层
	 * 并无其它功能，仅用于阻断，比如显示一个黑色背景 
	 * @author yinhunfeixue
	 * 
	 */	
	public class Modal extends SkinnableComponent
	{

		/**
		 * 实例化 
		 * 
		 */		
		public function Modal()
		{
			super();
			percentWidth = 100;
			percentHeight = 100;
		}
	}
}